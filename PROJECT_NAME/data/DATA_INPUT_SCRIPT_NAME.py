
def get_train_data():
    # Write how the model gets the training data, it will be called in
    # train_model.train(model=model,
    #                   train_data=get_train_data(),
    #                   eval_data=get_eval_data(),
    #                   config=config)
    return None

def get_eval_data():
    # Write how the model gets the eval data when training, it will be called in
    # train_model.train(model=model,
    #                   train_data=get_train_data(),
    #                   eval_data=get_eval_data(),
    #                   config=config)

    return None

